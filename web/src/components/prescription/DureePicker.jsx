/**
 * DuréePicker - French alias for DurationSelector
 * Composant de sélection de durée de traitement avec recherche et autocomplétion
 * 
 * @component
 * @example
 * <DuréePicker 
 *   value={duree} 
 *   onChange={setDuree}
 *   disabled={false}
 * />
 */
import DurationSelector from './DurationSelector';

export default DurationSelector;
