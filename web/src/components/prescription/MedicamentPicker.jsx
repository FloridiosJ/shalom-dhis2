/**
 * MédicamentPicker - French alias for MedicationSelector
 * Composant de sélection de médicament avec recherche et autocomplétion
 * 
 * @component
 * @example
 * <MédicamentPicker 
 *   value={medicament} 
 *   onChange={setMedicament}
 *   disabled={false}
 * />
 */
import MedicationSelector from './MedicationSelector';

export default MedicationSelector;
