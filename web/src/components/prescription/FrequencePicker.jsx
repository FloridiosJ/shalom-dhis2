/**
 * FréquencePicker - French alias for FrequencySelector
 * Composant de sélection de fréquence d'administration avec autocomplétion
 * 
 * @component
 * @example
 * <FréquencePicker 
 *   value={frequence} 
 *   onChange={setFrequence}
 *   disabled={false}
 * />
 */
import FrequencySelector from './FrequencySelector';

export default FrequencySelector;
